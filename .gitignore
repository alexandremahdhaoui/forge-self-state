# Patterns are anchored to the repo root on purpose. The state engine
# commits records under runs/<revision>/<stage>/..., and a stage can be
# named anything - "build" included. An unanchored build/ matched
# runs/<rev>/build/ and blocked every record write.
/.forge/
/.forge-ci/
/build/
.envrc
/tmp/
